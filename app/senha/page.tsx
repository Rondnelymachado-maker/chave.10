"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "../../lib/supabase";
export default function SenhaPage(){const router=useRouter();const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [ok,setOk]=useState("");const [loading,setLoading]=useState(false);useEffect(()=>{supabase.auth.getUser().then(({data})=>{if(!data.user)router.replace("/login")})},[router]);async function submit(e:FormEvent){e.preventDefault();setError("");setOk("");if(password.length<8){setError("Use uma senha com pelo menos 8 caracteres.");return}if(password!==confirm){setError("As senhas não conferem.");return}setLoading(true);const {error:e2}=await supabase.auth.updateUser({password});setLoading(false);if(e2){setError(e2.message);return}setPassword("");setConfirm("");setOk("Senha alterada com sucesso.");}return <main style={page}><section style={card}><div style={brand}>CHAVE <span>10</span></div><h1>Trocar senha</h1><p style={muted}>Defina uma nova senha para sua conta.</p><form onSubmit={submit}><label>Nova senha</label><input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mínimo: 8 caracteres" style={input}/><label>Confirmar nova senha</label><input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repita a senha" style={input}/>{error&&<div style={err}>{error}</div>}{ok&&<div style={success}>{ok}</div>}<button disabled={loading} style={button}>{loading?"Salvando...":"Alterar senha"}</button></form><button onClick={()=>router.replace("/")} style={back}>Voltar ao sistema</button></section></main>}
const page:React.CSSProperties={minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",background:"#f3f5f8",padding:20,fontFamily:"Arial,sans-serif",color:"#172033"};const card:React.CSSProperties={width:"100%",maxWidth:430,background:"white",borderRadius:16,padding:30,boxShadow:"0 12px 40px rgba(23,32,51,.12)"};const brand:React.CSSProperties={fontWeight:900,fontSize:26};const muted:React.CSSProperties={color:"#687386"};const input:React.CSSProperties={width:"100%",padding:12,margin:"6px 0 14px",border:"1px solid #d7dce3",borderRadius:8,boxSizing:"border-box"};const button:React.CSSProperties={width:"100%",marginTop:10,background:"#f59b32",color:"white",border:0,borderRadius:8,padding:13,fontWeight:800};const back:React.CSSProperties={width:"100%",marginTop:10,background:"white",border:"1px solid #d7dce3",borderRadius:8,padding:12,fontWeight:700};const err:React.CSSProperties={marginTop:8,padding:10,borderRadius:7,background:"#fff0f0",color:"#9b2226",fontSize:13};const success:React.CSSProperties={marginTop:8,padding:10,borderRadius:7,background:"#eefbf3",color:"#176b38",fontSize:13};