import { useEffect,useState } from "react";
import { ActivityIndicator,FlatList,StyleSheet,Text,View } from "react-native";
import { supabase } from "@/lib/supabase";
export default function Clientes(){const[items,setItems]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const{data:u}=await supabase.auth.getUser();if(!u.user)return setLoading(false);const{data:p}=await supabase.from("user_profiles").select("office_id").eq("user_id",u.user.id).single();if(!p?.office_id)return setLoading(false);const{data}=await supabase.from("clients").select("id,name,phone").eq("office_id",p.office_id).order("created_at",{ascending:false});setItems(data||[]);setLoading(false)})()},[]);if(loading)return <View style={s.center}><ActivityIndicator color="#f59b32"/></View>;return <FlatList style={s.screen} contentContainerStyle={s.content} data={items} keyExtractor={x=>x.id} ListHeaderComponent={<Text style={s.title}>Clientes</Text>} ListEmptyComponent={<Text style={s.empty}>Nenhum cliente cadastrado.</Text>} renderItem={({item})=><View style={s.card}><Text style={s.name}>{item.name}</Text><Text style={s.phone}>{item.phone||"Sem telefone"}</Text></View>}/>};const s=StyleSheet.create({screen:{flex:1,backgroundColor:"#f5f7fa"},content:{padding:18},center:{flex:1,alignItems:"center",justifyContent:"center"},title:{fontSize:26,fontWeight:"800",color:"#172033",marginBottom:14},card:{backgroundColor:"#fff",padding:16,borderRadius:12,borderWidth:1,borderColor:"#e5e7eb",marginBottom:10},name:{fontSize:16,fontWeight:"700",color:"#172033"},phone:{color:"#687386",marginTop:4},empty:{color:"#687386",paddingTop:20}});
